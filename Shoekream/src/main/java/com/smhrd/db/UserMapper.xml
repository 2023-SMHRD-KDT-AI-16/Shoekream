<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user.UserMapper">

    <select id="login" parameterType="map" resultType="int">
        SELECT userPassword FROM USER WHERE userID = #{userID}
    </select>
    
    <select id="registerCheck" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM USER WHERE userID = #{userID}
    </select>
    
    <insert id="register" parameterType="com.smhrd.model.UserDTO">
        INSERT INTO USER (userID, userPassword, userName, userAge, userGender, userEmail, userProfile)
        VALUES (#{userID}, #{userPassword}, #{userName}, #{userAge}, #{userGender}, #{userEmail}, #{userProfile})
    </insert>
    
    <select id="getUser" parameterType="string" resultType="com.smhrd.model.UserDTO">
        SELECT * FROM USER WHERE userID = #{userID}
    </select>
    
    <update id="update" parameterType="map">
        UPDATE USER SET userPassword = #{userPassword}, userName = #{userName}, userAge = #{userAge},
        userGender = #{userGender}, userEmail = #{userEmail} WHERE userID = #{userID}
    </update>
    
    <update id="profile" parameterType="map">
        UPDATE USER SET userProfile = #{userProfile} WHERE userID = #{userID}
    </update>
    
    <select id="getProfile" parameterType="string" resultType="string">
        SELECT userProfile FROM USER WHERE userID = #{userID}
    </select>
    
</mapper>